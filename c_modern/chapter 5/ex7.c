#include <stdio.h>

int main(void) {
    int a, b, c, d;

    printf("Enter 4 integers: ");
    scanf("%d %d %d %d", &a, &b, &c, &d);

    if(a > b){
        if(c > d) {

        }
    }

    return 0;
}

#include <stdio.h> // import contents of header file, which contain lists of functions

/* This is a comment. */
int main(int argc, char *argv[])
{
    int distance = 100; // declare and initialise

    // this is also a comment
    printf("You are %d miles away.\n", distance);

    return 0; // return from main function, give OS exit value
}
